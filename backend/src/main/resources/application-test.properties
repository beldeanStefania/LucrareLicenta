# Use localhost for tests
spring.datasource.url=jdbc:mysql://localhost:3306/orar
spring.datasource.username=root
spring.datasource.password=3676

# Let Hibernate create/drop tables for tests (adjust as needed)
spring.jpa.hibernate.ddl-auto=create-drop

# Other properties (logging, dialect, etc.)
spring.jpa.show-sql=true
